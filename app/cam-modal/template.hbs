{{#md-modal close="toggleModal"}}
  <div class="card">
    <div class="card-image">
      <img src="{{selectedCam.imageSource}}">
    </div>
    <div class="card-content">
      <span class="card-title grey-text text-darken-4">
        {{selectedCam.name}}
      </span>
      <p>{{selectedCam.location}}</p>
    </div>
  </div>
{{/md-modal}}