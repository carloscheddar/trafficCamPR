/* jshint expr:true */
import { expect } from 'chai';
import {
  describe,
  it
} from 'mocha';
import cams from 'traffic-cam-pr/utils/cams';

describe('cams', function() {
  // Replace this with your real tests.
  it('works', function() {
    var result = cams();
    expect(result).to.be.ok;
  });
});
